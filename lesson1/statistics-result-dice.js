// ici on récupère les élements HTML
var start = document.getElementById('start');

start.addEventListener('click', function() {
  // 1- récupérer le nombre de lancer
  // 2- lancer le dés x fois et collecter les resultats
  // 3- afficher les resultats en HTML
  displayResult();
});

function displayResult() {
  // aide : modifier les heights des div : col1, col2, col3 etc
}
